import React from 'react'import {Link} from "react-router-dom";const ArticleItem = ({article, articleDelete}) => {    return (        <tr>            <td>{article.id}</td>            <td>                <Link to={`article/${article.id}`}>                {article.name}                </Link>            </td>            <td>                <Link to={`user/${article.author.id}`}>                    {article.author.first_name} {article.author.last_name}                </Link>            </td>            <td>                <button onClick={() => articleDelete(article.id)}>                    Delete                </button>            </td>        </tr>    )}const ArticleList = ({articles, articleDelete}) => {    return (        <div>            <table>                <thead>                <tr>                    <th>ID</th>                    <th>NAME</th>                    <th>AUTHOR</th>                    <th></th>                </tr>                </thead>                <tbody>                {articles.map((article) => <ArticleItem key={article.id} article={article} articleDelete={articleDelete}/>)}                </tbody>            </table>            <p><Link to="/articles/create">New article</Link></p>            <Link to="/articles/search">Search</Link>        </div>    )}export default ArticleList